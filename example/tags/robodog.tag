<!-- Riot tag definitinon -->
<robotdog class="instance">
    <h3> When robots take over </h3>
    <img src={ opts.img } >
    <ul>
        <li>{ opts.type }</li>
        <li>{ opts.name }</li>
        <li>{ opts.avgSize }</li>
        <li>{ opts.tail }</li>
        <li>{ opts.brain }</li>
    </ul>
</robotdog>